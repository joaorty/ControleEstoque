#ifndef LUCRO_H
#define LUCRO_H
#include <stdio.h>

void atualizarLucro(float lucro, FILE *l); //Atualiza o lucro assim que se vende
float verLucro(float lucro, FILE *l); //Retorna o valor do lucro atual

#endif // LUCRO_H
